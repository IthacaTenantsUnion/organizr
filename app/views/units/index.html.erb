<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>Landlord(s) Known</th>
    </tr>
  </thead>

  <tbody>
    <% @units.each do |unit| %>
      <tr>
        <td><%= link_to unit.address, unit %></td>
        <td>
          <% unit.landlords.to_a.map do |l| %>
            <%= link_to l.name, l %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>